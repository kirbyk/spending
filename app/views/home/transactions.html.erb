<h2>Transactions</h2>

<table class="table table-striped">
  <% @account[:transactions].each do |t| %>
    <tr>
      <td><%= t["name"] %></td>
      <td><%= t["amount"] %></td>
    </tr>
  <% end %>
</table>
