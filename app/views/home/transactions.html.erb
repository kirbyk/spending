<h2>Transactions</h2>

<table class="table table-striped">
  <th>Date</th>
  <th>Name</th>
  <th>Amount</th>
  <% @transactions.each do |t| %>
    <tr>
      <td><%= t["date"] %></td>
      <td><%= t["name"] %></td>
      <td><%= t["amount"] %></td>
    </tr>
  <% end %>
</table>
